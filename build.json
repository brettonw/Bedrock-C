{
    "values": {
    	"sourcePath": "source"
    },
    "configurations": {
        "debug": {
            "compilerOptions": "-Wno-psabi $compilerOptions",
            "linkerOptions": "-lpthread -lcurl"
        },
        "release": {
            "compilerOptions": "-Wno-psabi $compilerOptions",
            "linkerOptions": "-lpthread -lcurl"
        }
    },
    "tools": {
        "cloc": "echo 'SOURCES'; wc -l `find $sourcePath -name '*.[hc]*' | grep -v '+test'` | sort; echo 'TEST'; wc -l `find $sourcePath -name '*.[hc]*' | grep '+test'` | sort",
        "status": "git status"
    }
}
